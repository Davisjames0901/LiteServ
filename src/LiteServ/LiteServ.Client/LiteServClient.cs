namespace LiteServ.Client
{
    public class LiteServClient
    {
        public TT Send<T, TT>(string path, T payload)
        {
            return default;
        }
        public void Send<T>(string path, T payload)
        {
            
        }
    }
}